const name= "ankita"
const repo =40
console.log(`my name is ${name} and my number of repo is ${repo}`)

const keyvalue = new String('ANKITA')
console.log(keyvalue[0]);
console.log(keyvalue.__proto__);
console.log(keyvalue.charAt(2))
console.log(keyvalue.indexOf('T'))

const slicing = keyvalue.slice(-6,4);
console.log(slicing)

